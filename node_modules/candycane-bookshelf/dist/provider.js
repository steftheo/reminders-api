'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = undefined;

var _emberCliStringUtils = require('ember-cli-string-utils');

var _provider = require('candycane/dist/foundation/provider');

var _provider2 = _interopRequireDefault(_provider);

var _bookshelf = require('bookshelf');

var _bookshelf2 = _interopRequireDefault(_bookshelf);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

let KnexProvider = class KnexProvider extends _provider2.default {
  register() {
    const db = this.app.make(`db`);

    const store = (0, _bookshelf2.default)(db);
    store.plugin(`registry`);

    this.app.pathsForNamespace(`models`).forEach(modelNode => {
      const modelModule = require(modelNode.fullPath);
      const modelDefinition = modelModule.default || modelModule;

      store.model(modelNode.module, modelDefinition);
    });

    this.app.singleton(`store`, store);
  }
};
exports.default = KnexProvider;